<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Server action: clear phantom breaks on records with no real session -->
    <record id="action_clear_invalid_breaks" model="ir.actions.server">
        <field name="name">Clear Invalid Breaks</field>
        <field name="model_id" ref="hr_attendance.model_hr_attendance"/>
        <field name="state">code</field>
        <field name="code">
n = model.action_clear_invalid_breaks()
action = {
    'type': 'ir.actions.client',
    'tag': 'display_notification',
    'params': {
        'title': 'Clear Invalid Breaks',
        'message': '%s record(s) cleaned.' % n,
        'type': 'success',
        'sticky': False,
    },
}
        </field>
    </record>

</odoo>
